# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Model checkpoints and other large files
*.pt
datasets/

# Local environment overrides
.env
.vscode
.venv

models/
lightning_logs/
datasets/

# Folders with logs of runs
runs
da_results
wandb
# This is a symbolic link
datasets
# This is a folder where I store the splits of the dataset, but it depends a lot on the dataset, so I will ignore it for now
tools/splits
results_threshold
